# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(orb_slam2TaskLib)
ADD_LIBRARY(${ORB_SLAM2_TASKLIB_NAME} SHARED 
    ${ORB_SLAM2_TASKLIB_SOURCES})
add_dependencies(${ORB_SLAM2_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${ORB_SLAM2_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${ORB_SLAM2_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ORB_SLAM2_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ORB_SLAM2_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${ORB_SLAM2_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${ORB_SLAM2_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ORB_SLAM2_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ORB_SLAM2_TASKLIB_HEADERS}
    DESTINATION include/orocos/orb_slam2)

